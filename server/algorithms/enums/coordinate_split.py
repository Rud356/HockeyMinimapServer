from enum import IntEnum, auto


class HorizontalPosition(IntEnum):
    top = auto()
    bottom = auto()
    center = auto()


class VerticalPosition(IntEnum):
    left = auto()
    right = auto()
    center = auto()
